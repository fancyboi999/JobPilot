# 项目结构说明

## 📂 目录树

```
JobPilot/
├── src/                      # 【源代码目录 - 在这里开发】
│   ├── index.js             # 主入口文件
│   ├── config.js            # 配置管理
│   ├── state.js             # 状态管理
│   ├── utils.js             # 工具函数
│   ├── ui.js                # UI 组件
│   ├── core.js              # 核心业务逻辑
│   ├── letter.js            # 欢迎信模块
│   ├── guide.js             # 引导模块
│   └── settings.js          # 设置模块
│
├── motherduckstyle/          # 样式资源
│   └── motherduck-ui-theme.css
│
├── 文档/
│   ├── BUILD_GUIDE.md       # 快速构建指南
│   ├── DEVELOPMENT.md       # 完整开发文档
│   ├── 构建系统总结.md      # 构建系统说明
│   ├── README.md            # 项目简介（中文）
│   └── README.en.md         # 项目简介（英文）
│
├── 配置文件/
│   ├── main.js              # 油猴脚本元数据头部
│   ├── rollup.config.js     # Rollup 构建配置
│   ├── package.json         # npm 项目配置
│   └── .gitignore           # Git 忽略文件
│
├── JOBPILOT.js              # 【构建产物 - 发布用】
└── LICENSE                  # 开源协议
```

## 📝 文件说明

### 核心文件

| 文件 | 说明 | 是否修改 |
|------|------|---------|
| `src/*.js` | 源代码模块 | ✅ 经常修改 |
| `JOBPILOT.js` | 构建产物 | ❌ 自动生成 |
| `main.js` | 元数据头部 | 🔧 仅改版本号 |
| `rollup.config.js` | 构建配置 | 🔧 特殊需求才改 |
| `package.json` | 项目配置 | 🔧 添加依赖时改 |

### 工作流程

```
1. 开发    →  编辑 src/ 下的文件
2. 构建    →  运行 npm run build
3. 测试    →  导入 JOBPILOT.js 到浏览器
4. 发布    →  提交 JOBPILOT.js
```

## 🎯 快速参考

### 添加新功能
```bash
# 1. 在 src/ 创建新模块
vim src/new-feature.js

# 2. 在 src/index.js 导入
echo "import './new-feature.js';" >> src/index.js

# 3. 构建
npm run build
```

### 修改现有功能
```bash
# 1. 编辑对应模块
vim src/ui.js

# 2. 自动构建（推荐）
npm run dev
```

### 更新版本
```bash
# 1. 修改版本号
vim main.js  # 第 4 行

# 2. 重新构建
npm run build

# 3. 提交发布
git add .
git commit -m "chore: bump version to x.x.x"
```

## ⚠️ 注意事项

### ✅ 应该做的
- 在 `src/` 目录下开发和修改代码
- 使用 `npm run dev` 开发模式
- 修改版本号时编辑 `main.js`
- 发布前运行 `npm run build`

### ❌ 不应该做的
- 直接修改 `JOBPILOT.js`（会被覆盖）
- 删除 `src/` 目录下的文件
- 忘记运行构建就发布

---

更多详情请查看 [DEVELOPMENT.md](DEVELOPMENT.md)
